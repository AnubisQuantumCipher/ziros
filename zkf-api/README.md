# zkf-api

`zkf-api` exposes ZirOS as a service-oriented HTTP API. It wraps proving,
deployment, queueing, and related server-side flows for remote or multi-tenant
usage.

## Public API Surface

- Binary: `zkf-api`
- Scope: HTTP API server, request handling, queueing, deployment endpoints
