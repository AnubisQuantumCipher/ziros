# zkf-registry

`zkf-registry` manages gadget publication, listing, versioning, and resolution
inside ZirOS. It is the package-management layer for reusable proving gadgets
and registry-backed dependency graphs.

## Public API Surface

- Library crate: `zkf_registry`
- Responsibilities: manifests, publish/list flows, dependency resolution
