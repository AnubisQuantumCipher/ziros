# zkf-conformance

`zkf-conformance` defines the backend conformance corpus used to check that
compile/prove/verify behavior stays aligned across backend implementations. It
is the source material behind `zkf-cli conformance`.

## Public API Surface

- Library crate: `zkf_conformance`
- Contents: conformance programs, expected outputs, backend compatibility checks
