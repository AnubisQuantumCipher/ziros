# zkf-runtime

`zkf-runtime` implements the Unified Memory Prover Graph (UMPG) control plane,
telemetry-backed scheduling, trust-lane routing, and runtime policy evaluation
used by the CLI and higher-level proving flows.

## Public API Surface

- Library crate: `zkf_runtime`
- Responsibilities: planning, execution routing, telemetry, scheduler policy,
  backend recommendation
