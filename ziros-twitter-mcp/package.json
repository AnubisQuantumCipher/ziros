{
  "name": "ziros-twitter-mcp",
  "version": "1.0.0",
  "private": true,
  "files": [],
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "pack:audit": "npm pack --json --dry-run",
    "test": "npm run build && node --test build/test/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
