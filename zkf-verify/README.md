# zkf-verify

`zkf-verify` packages verification-focused library code and a standalone
verification binary for proof artifacts. It is the narrow verifier-side surface
for deployments that do not need the full proving toolchain.

## Public API Surface

- Library crate: `zkf_verify`
- Binary: `zkf-verify`
- Scope: proof verification helpers and verifier-side artifact inspection
