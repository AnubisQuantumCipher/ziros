# zkf-tui

`zkf-tui` packages the dashboard shell, widgets, and local proof-worker helpers
used by the standalone TUI app style. It is the terminal-native UI layer for
interactive ZirOS applications.

## Public API Surface

- Library crate: `zkf_tui`
- Main surfaces: dashboard state, widgets, proof job helpers, reference app
  scaffolding
