# zkf-ui

`zkf-ui` contains reusable terminal rendering helpers for ZirOS proof flows.
It is the presentation layer behind the colored standalone app scaffold and
other CLI-adjacent proof progress surfaces.

## Public API Surface

- Library crate: `zkf_ui`
- Main surfaces: proof banners, progress rendering, check/proof result renderers
