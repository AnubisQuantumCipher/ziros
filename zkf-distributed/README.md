# zkf-distributed

`zkf-distributed` implements multi-node coordination for distributed proving and
cluster-aware execution. It works with the runtime and swarm layers to keep
distributed orchestration explicit and inspectable.

## Public API Surface

- Library crate: `zkf_distributed`
- Scope: peer discovery, distributed coordination, cluster worker logic
