# EPA Fixture

This fixture backs the Getting Started tutorial and the built-in audit script.

- `zirapp.json`: the EPA water discharge compliance app spec
- `inputs.compliant.json`: a passing witness input set
- `inputs.violation.json`: an intentionally failing witness input set
