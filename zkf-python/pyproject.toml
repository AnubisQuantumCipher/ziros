[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "zkf"
version = "0.1.0"
description = "Python bindings for ZirOS (formerly ZKF)"
requires-python = ">=3.8"

[tool.maturin]
module-name = "zkf"
